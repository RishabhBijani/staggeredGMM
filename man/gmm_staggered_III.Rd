% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_staggered_III.R
\name{gmm_staggered_III}
\alias{gmm_staggered_III}
\title{GMM III: Staggered Treatment Effect Estimator (unrestricted within-cohort covariance weighting)}
\usage{
gmm_staggered_III(
  data,
  y_var,
  t_var,
  id_var,
  gname,
  has_nt = FALSE,
  max_iter = 10L,
  covar = NULL
)
}
\arguments{
\item{data}{A data frame (or data.table) containing a balanced panel: one
row per unit-period, with every unit observed in every period.}

\item{y_var}{Character. Name of the outcome variable column in \code{data} --
the variable whose treatment effect is being estimated.}

\item{t_var}{Character. Name of the calendar-time column in \code{data} (e.g.
year or quarter), coded as consecutive integers.}

\item{id_var}{Character. Name of the column in \code{data} that uniquely
identifies each cross-sectional unit (e.g. individual, firm, county)
tracked over time.}

\item{gname}{Character. Name of the cohort (treatment-timing group)
column in \code{data}: each unit's first treated period, with \code{0} reserved
for units that are never treated.}

\item{has_nt}{Logical, default \code{FALSE}. If \code{TRUE}, units with \code{gname == 0}
are included as a never-treated control group, in addition to (not
instead of) not-yet-treated cohorts, which always serve as controls
whenever available regardless of \code{has_nt}. Setting this to \code{TRUE} when a
never-treated group exists typically improves precision and identifies
more CATTs.}

\item{max_iter}{Integer, default \code{10L}. Maximum number of iterated-GMM
reweighting steps: the moment-covariance model is re-estimated from the
current CATT estimates and the weighting is updated, repeating until the
estimates stop changing appreciably or this cap is reached.}

\item{covar}{Optional character vector of baseline (pre-treatment,
time-invariant) covariate column names in \code{data}, e.g. \code{c("x1", "x2")}.
Default \code{NULL} (no covariate adjustment; \code{""} or \code{character(0)} are
treated identically to \code{NULL}). When supplied, applies the
outcome-regression adjustment of Section 4.5 of the paper, which allows
parallel trends to hold only after conditioning on \code{covar} rather than
unconditionally.}
}
\value{
A list with the same components as \code{\link[=gmm_staggered_I]{gmm_staggered_I()}}. The
per-cohort unrestricted covariance matrices underlying this estimator's
weighting are used internally but are not part of the returned value.
}
\description{
Estimates cohort x time-specific average treatment effects
(CATTs) under staggered adoption by generalized method of moments (GMM),
using only clean 2x2 DiD comparisons as moment conditions. Each cohort's
within-cohort T x T covariance is left fully unrestricted (no
stationarity assumed), estimated directly from that cohort's own
residuals, with no cross-cohort covariance terms. See
\code{\link[=gmm_staggered_I]{gmm_staggered_I()}} (pooled Toeplitz) and \code{\link[=gmm_staggered_II]{gmm_staggered_II()}}
(cohort-specific Toeplitz) for the other two weightings.

Arora, P. and Bijani, R. (2026). "Efficient Estimation of Treatment
Effects under Staggered Adoption: GMM Approach." Ashoka University
Economics Discussion Paper 163.
\url{https://dp.ashoka.edu.in/ash/wpaper/paper163_0.pdf}
}
\details{
At typical cohort sample sizes, estimating an unrestricted T x T
covariance per cohort routinely leaves the efficient GMM step unable to
converge (\code{eff_ok = FALSE}); the fallback sandwich standard error is the
norm rather than the exception for this estimator, not a sign of
misuse. Because of this, and unlike \code{\link[=gmm_staggered_I]{gmm_staggered_I()}} and
\code{\link[=gmm_staggered_II]{gmm_staggered_II()}}, \code{eff_ok = FALSE} does not raise a warning here --
a warning on nearly every call to this estimator would just be noise.

This "FullCov" weighting relaxes the \emph{within}-cohort structure (no
stationarity assumed) while keeping cohorts mutually independent. It is
a different, non-nested generalisation of the pooled-Toeplitz baseline
from one that instead relaxes \emph{cross}-cohort independence while keeping
the within-cohort Toeplitz structure -- the two are not interchangeable
and are not expected to produce the same estimates.
}
\section{Warnings}{
 \code{Q_H} (the incidence matrix mapping clean comparisons
to CATTs) can be rank deficient under some designs -- most commonly when
a cohort has no later cohort available as a not-yet-treated control and
\code{has_nt = FALSE}. When this happens, the affected CATTs have no clean
comparison at all, so the fallback reports \verb{beta_hat = 0, SE = 0} for
exactly these cells -- not a precisely-estimated null -- and a warning
names the exact affected cells. \code{has_nt = TRUE} with no \code{gname == 0}
units present in \code{data} is treated as a likely data-coding error and
raises an error rather than silently proceeding as \code{has_nt = FALSE}.
Non-convergence of the efficient GMM step (\code{eff_ok = FALSE}) does not
raise a warning here (see Details). When \code{covar} is supplied, any
(control cohort, pre-period, post-period) triple whose covariate
adjustment cannot be identified reverts to the unconditional comparison
for that triple only, and a warning reports how many contrasts were
affected; a separate warning names any units whose covariate value is
not actually constant over time.
}

\examples{
\donttest{
res <- gmm_staggered_III(sim_panel, y_var = "y", t_var = "year",
                          id_var = "unit_id", gname = "cohort",
                          has_nt = TRUE)
res$ATT_CW
res$eff_ok
}

}
